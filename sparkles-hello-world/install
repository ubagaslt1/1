#!/bin/bash
# Copy app to Umbrel apps directory
cp -r /tmp/xmrig-app/* ${APP_DIR}/

# Set permissions
chmod +x ${APP_DIR}/scripts/*.sh

# Create data directory
mkdir -p ${APP_DATA_DIR}/config

echo "XMRig app installed successfully!"
